<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:naver="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 네이버 지도 Fragment -->
    <fragment
        android:id="@+id/map"
        android:name="com.naver.maps.map.MapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- 현재 위치 버튼 -->
    <Button
        android:id="@+id/current_location_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="현재 위치"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_margin="16dp" />

    <!-- 실내 지도로 전환 버튼 -->
    <Button
        android:id="@+id/switch_to_indoor_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="실내 지도로 전환"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@id/current_location_btn"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp" />

    <!-- Project B 시작 버튼 -->
    <Button
        android:id="@+id/start_project_b_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Project B 시작"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@id/switch_to_indoor_btn"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp" />

    <!-- 건물 모서리 좌표를 표시할 TextView -->
    <TextView
        android:id="@+id/building_corner_coordinates"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="건물 모서리 좌표"
        android:padding="16dp"
        android:textSize="16sp"
        android:textColor="@android:color/black"
        android:background="#AAFFFFFF"
        android:layout_alignParentBottom="true" />

</RelativeLayout>
