<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- 네이버 지도 뷰 -->
    <com.naver.maps.map.MapView
        android:id="@+id/mapView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="내비게이션 지도. 화면을 탭하면 현재 위치를 음성으로 안내합니다."
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 상태 표시 패널 -->
    <androidx.cardview.widget.CardView
        android:id="@+id/statusPanel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        app:cardCornerRadius="8dp"
        app:cardElevation="4dp"
        app:layout_constraintTop_toTopOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- 환경 상태 -->
            <TextView
                android:id="@+id/environmentStatus"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="18sp"
                android:textStyle="bold"
                android:importantForAccessibility="yes"
                tools:text="현재 환경: 실외" />

            <!-- GPS 신호 강도 -->
            <TextView
                android:id="@+id/gpsSignalStatus"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:importantForAccessibility="yes"
                tools:text="GPS 신호: 양호" />

            <!-- 정확도 -->
            <TextView
                android:id="@+id/accuracyStatus"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:importantForAccessibility="yes"
                tools:text="정확도: 5m" />
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- 접근성 컨트롤 패널 -->
    <androidx.cardview.widget.CardView
        android:id="@+id/accessibilityPanel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        app:cardCornerRadius="8dp"
        app:cardElevation="4dp"
        app:layout_constraintBottom_toBottomOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- 현재 위치 음성 안내 버튼 -->
            <Button
                android:id="@+id/announceLocationButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="현재 위치 안내"
                android:textSize="18sp"
                android:padding="16dp"
                android:contentDescription="현재 위치를 음성으로 안내합니다."
                style="@style/Widget.MaterialComponents.Button" />

            <!-- 방향 음성 안내 버튼 -->
            <Button
                android:id="@+id/announceDirectionButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="이동 방향 안내"
                android:textSize="18sp"
                android:padding="16dp"
                android:contentDescription="현재 이동 방향을 음성으로 안내합니다."
                style="@style/Widget.MaterialComponents.Button" />
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- 로딩 인디케이터 -->
    <ProgressBar
        android:id="@+id/loadingIndicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>